# Orchestrator-workers workflow

Adapted from the Spring AI orchestrator-workers example.

The orchestrator returns two structured tasks. Two worker calls then generate
formal and conversational announcements. The test verifies the decomposition,
worker ordering, prompt propagation, and final response.
